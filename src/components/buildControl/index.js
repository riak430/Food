import React from "react";

function BuildControl(props) {
  return (
    <div>
      <p>{props.title}</p>
      <button>Нэмэх</button>
      <button>Хасах</button>
    </div>
  );
}

export default BuildControl;
